/* eslint-env mocha */

import { Meteor } from "meteor/meteor";

if (Meteor.isServer) {
  describe("reminders", () => {
    describe("methods", () => {
      it("can delete owned reminder", () => {});
    });
  });
}
